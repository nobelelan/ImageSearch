<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <ProgressBar
        android:id="@+id/pbLoadState"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvErrorLoadState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Results could not be loaded"
        app:layout_constraintEnd_toEndOf="@+id/pbLoadState"
        app:layout_constraintStart_toStartOf="@+id/pbLoadState"
        app:layout_constraintTop_toBottomOf="@+id/pbLoadState" />

    <Button
        android:id="@+id/btnRetryLoadState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Retry"
        app:layout_constraintEnd_toEndOf="@+id/tvErrorLoadState"
        app:layout_constraintStart_toStartOf="@+id/tvErrorLoadState"
        app:layout_constraintTop_toBottomOf="@+id/tvErrorLoadState" />
</androidx.constraintlayout.widget.ConstraintLayout>